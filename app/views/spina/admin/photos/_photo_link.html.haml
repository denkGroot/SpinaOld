#modal.modal
  %header.big-header
    = link_to '#', class: 'close_modal', data: {dismiss: 'modal'} do
      %i.icon-only{data: {icon: 'x'}}

    %h3
      De link naar de afbeelding is:
      %strong= @photo.file.url

  %footer
    = link_to 'Ok, venster sluiten', '#', data: {dismiss: 'modal', icon: 'v'}, style: 'width: 100%'
